"use strict";

class Task {
    titulo: string;
    desc: string;
    dueTo: string;
    constructor (titulo: string, desc: string, dueTo: string) {
        this.titulo = titulo;
        this.desc = desc;
        this.dueTo = dueTo;
    }
}
